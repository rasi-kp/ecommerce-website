
{{> adminheader}}

<div class="content-wrapper">
  <div class="container-xxl flex-grow-1 container-p-y">
    <h4 class="py-1 mb-2">Order ID : {{orders.orderID}}</h4>
    <h4 class="py-2 mb-0">Payment ID : {{paymentID}}<a href="https://dashboard.razorpay.com/app/payments" target="_blank">https://dashboard.razorpay.com/app/payments</a></h4>
    <h4 class="py-2 mb-0">Order Date : {{orders.orderdate}}</h4>
    <h4 class="py-2 mb-0">Name : {{orders.name}}</h4>
    <h4 class="py-2 mb-0">Address : {{orders.address}}</h4>
    <h4 class="py-2 mb-0">City : {{orders.city}}</h4>
    <h4 class="py-2 mb-0">State : {{orders.state}}</h4>
    <h4 class="py-2 mb-0">Pincode : {{orders.pincode}}</h4>
    <h4 class="py-2 mb-2">Total Amount : {{orders.totalamount}}</h4>

    <div class="row">
      <div class="col-md-12">

        <div class="card mb-0">
          {{#each orders.items}}
          <h5 class="card-header">Product Details </h5>
          <!-- Account -->
          <div class="card-body">
            <div class="d-flex align-items-start align-items-sm-center gap-4">
              <img src="../products-images/{{this.product.image}}" alt="user-avatar" class="d-block rounded"
                height="100" width="100" id="uploadedAvatar" />
              <div class="button-wrapper">
                <label for="upload" class="btn btn-primary me-2 mb-4" tabindex="0">
                  <span class=""> Name : {{this.product.name}}</span>
                
                </label>
                
              </div>
            </div>
          </div>
          <hr class="my-0" />
          <div class="card-body">

              <div class="row">

                <div class="mb-3 col-md-6">
                  <label for="firstName" class="form-label">Quantity :</label>
                  <input class="form-control" placeholder="1" type="text" value="{{this.product.quantity}}" name="username" readonly />
                </div>
                <div class="mb-3 col-md-6">
                  <label for="lastName" class="form-label">Size :</label>
                  <input class="form-control" type="text" name="name" value="{{this.size}}" placeholder="size" readonly />
                </div>
                <div class="mb-3 col-md-6">
                  <label for="lastName" class="form-label">Price :</label>
                  <input class="form-control" name="prize" placeholder="Rs :" value="Rs :{{this.product.price}}" id="price"
                    readonly />
                </div>
                <div class="mb-3 col-md-6">
                  <label for="lastName" class="form-label">category:</label>
                  <input class="form-control" type="email" name="category" placeholder="Category"value="{{this.product.category}}"
                    readonly />
                </div>

               
          </div>
          {{/each}}
          
          <!-- /Account -->
        </div>
        
      </div>
      <a href="/admin/orders" class="btn btn-primary mt-3 mb-4 text-white float-end">Back</a>
    </div>
    
  </div>
  
  {{> adminfooter}}